# BreakoutRoom 08.1

- Vytvořte novou stránku `/ahojSvete`
- Upravte `<PageTitle>`
- Vytvořte `@code` block, ve kterém definujete funkci `AhojSvete()`, která vrací string
- Funkci `AhojSvete()` zavolejte z těla `<body>` pomocí `@AhojSvete()`
- Otestujte v prohlížeči a v developer tools zkontrolujte, jaké nám chodí `HTML`
- Stránku nastrukturujte pomocí `HTML`
